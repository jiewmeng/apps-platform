var

module.exports = {
	/**
	 * Associates a social media login with a user account
	 *
	 * @param  {string} userId Application user ID
	 * @param  {string} network One of {facebook,...}
	 * @param  {string} networkUserId UserID from the social network
	 * @return {Promise} resolved when DB save is complete
	 */
	associateSocialLogin: function(userId, network, networkUserId) {
		
	}
};
